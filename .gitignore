# Arduino build output
**/build/

# Host unit-test binaries (test/test_foo executables; keep test_foo.c sources)
test/test_*
!test/test_*.c
test/*.dSYM/

# macOS / editor cruft
.DS_Store

# LoraLink WiFi credentials (copy lora_secrets.h.example -> lora_secrets.h)
LoraLink/lora_secrets.h

# ESP-IDF (KitchenSync)
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
.worktrees/
.gstack/
tests/test_x32_port
*.dSYM/
KitchenSyncTouch/secrets.h

# Web-flasher site: built by tools/build-flasher-site.sh, published by the release
# workflow. Firmware binaries are release artifacts, never repo contents.
site/

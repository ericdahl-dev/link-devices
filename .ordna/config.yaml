# Task prefixes in use: LNK (X32Link), MCK (MidiClock, legacy), ESP (emulator),
#                       ARC (firmware architecture), P4 (ESP32-P4 hub tier)
tasksDir: tasks
idPrefix: ESP
zeroPaddedIds: 3
statuses: [todo, doing, done]

---
id: P4-011
title: "MIDI clock IN → Link — bridge incoming clock to the session (other direction)"
status: todo
assignee: null
priority: low
tags: [ready-for-human, p4, hub, midi, link, clock-in]
depends_on: [P4-003, P4-004]
created_at: 2026-07-05
updated_at: 2026-07-11
---

## Goal

The other direction: derive tempo from **incoming MIDI clock** (from a USB-MIDI
device on the host port, or DIN) and **publish it into the Ableton Link session**,
so a hardware sequencer / drum machine can be the tempo master and pull Link (and
everything on it) along. Makes the P4 a two-way bridge, not just Link→MIDI.

## Reuse

- BPM-from-clock math already exists, pure + host-tested: `midi_bpm_calc.c`
  (24-PPQN pulse intervals → BPM), used by the S3 USB-MIDI-device path.
- USB-MIDI **host** IN reception is already wired (P4-005 reads the bulk IN
  endpoint for the loopback counter) — feed those 0xF8 bytes to `midi_bpm_calc`.

## Scope

- Feed host-IN 0xF8 timing to `midi_bpm_calc` → BPM.
- Publish tempo into the Link session (requires bidirectional Link — the P4
  becoming a tempo-setting peer, not just a listener; ties to the broader
  full-peer Link work).
- Handle source arbitration: Link vs MIDI-in as tempo master (web UI selectable).

## Deliverable

A MIDI-clock source on the P4's host port sets the Link session tempo; other Link
peers follow. Source selection in the web UI (P4-007).

## Notes

- The Link-publish side is the hard part (full peer / tempo-setting), bigger than
  just reading clock. Low priority until the read/clock-out spine is mature.

## Progress — re-cut in two stages (2026-07-05)

The original single-PR attempt (#21) was closed when the ARC-006/007 refactors
rewrote its files; re-cutting fresh on current master in two verifiable stages.

- **Stage 1 — MIDI clock IN -> BPM (done):** pure `midi_clock_in` (rings 24-PPQN
  0xF8 timestamps, BPM via the reused host-tested `midi_bpm_calc`, stop-timeout);
  `usb_midi_host` IN callback feeds timestamped pulses; detected tempo shown in
  `/status` (`"min"`) + a "MIDI Clock In" web readout. Host-tested
  (`test_midi_clock_in`). Verifiable alone: feed the host port a clock, watch BPM.
- **Stage 2 — publish into Link (todo):** the P4 becomes a tempo-setting peer —
  pure `link_proto_build_alive` + `link_proto_session_id`, `midi_link_master`
  timeline (take over an observed session, keeping beats continuous), a
  `wifi_link_send_alive` broadcast, and a `tempo_source` arbiter (Link vs MIDI-in)
  with a web selector. The hard part; verify against the Link reference-probe rig.
  Reference implementations exist in the closed #21 diff.

## Triage — 2026-07-11

> *This was generated by AI during triage.*

**`ready-for-human` (sequencing).** Stage 1 shipped. Stage 2 — the P4 as a tempo-SETTING Link peer — is clean-room protocol work whose correctness cannot be established by host tests alone; it needs a real-peer rig (P4-016, or the ad-hoc Mac probe) watching adoption/takeover. Owner call on sequencing: stand up P4-016 first, or accept Mac-rig verification and re-triage this to agent (reference impl exists in the closed PR #21 diff).

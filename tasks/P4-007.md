---
id: P4-007
title: "KitchenSync web UI — rack-panel config + live status, styled like X32Link"
status: done
assignee: null
priority: high
tags: [p4, hub, web, config, ui]
depends_on: [P4-005]
created_at: 2026-07-05
updated_at: 2026-07-05
---

## Goal

Give KitchenSync a web UI that matches the shipped **X32Link rack-panel aesthetic**
(dark rack unit, screws, green-LED accent `#b6ff36`, DSEG 7-seg tempo readout,
segmented controls / toggles / "Write & Reboot" button; see
`X32Link/web_config.cpp`). It's the P4's config surface per ADR-0004 (web UI =
full config; touch/real-time is separate) and the home for the Multiclock-style
per-output settings (P4-010).

## Scope (v1)

- **esp_http_server** (ESP-IDF) glue — the P4 is ESP-IDF, so not the Arduino
  `WebServer` X32Link uses. Serve `/` (page), `/status` (JSON), `/save` (POST).
- **Live status**, styled like X32Link: 7-seg **Link BPM**, beat dot, peers,
  USB-MIDI device state, clock running/TX. Poll `/status` at 1 Hz (reuse the
  X32Link JS shape).
- **Runtime config → NVS** (new for KitchenSync): move WiFi SSID/pass off compile-time
  Kconfig to web-set + NVS; clock-out enable + USB cable select. Save + reboot.
- **Pure, host-tested builders** per ADR-0003: page/status render + form parse in
  a `.c` with a Unity suite; `esp_http_server` is thin glue.

## Reuse

Lift the X32Link HTML/CSS template + the `/status` JS (phase-locked beat dot) and
the `web_status_json.c` pattern. Fonts load from CDN when online (same as
X32Link), mono fallback on the first-boot SoftAP.

## Deliverable

KitchenSync serves a rack-panel page on its IP showing live Link tempo + device state,
with WiFi + clock config saved to NVS. Verified in a browser on the network.

## Progress (2026-07-05) — v1 status page live ✅

Rack-panel **status page** serving on the P4 (`esp_http_server`), styled like
X32Link (dark unit, screws, green LED, `P4·HUB` wordmark, DSEG 7-seg tempo
readout, CONNECTED pill). Live via 1 Hz `/status` poll: Link BPM (132.0 verified
in-browser), peers, USB-MIDI device state, clock pulses. `/status` JSON is the
pure, host-tested `ks_status.c` (red-green TDD'd) — `esp_http_server` +
static HTML is thin glue (`ks_web.cpp`, aesthetic lifted from `web_config.cpp`).

## Complete (2026-07-05) ✅

Config surface done + verified on hardware:
- **Runtime config in NVS** — pure `ks_config.c` (struct/defaults/validate/
  per-field apply, red-green TDD'd via `test_ks_config`) + `ks_config_nvs.c`
  glue. WiFi creds are now runtime (off compile-time Kconfig; a Kconfig value only
  *seeds* first boot as a dev convenience).
- **Web config form** — WiFi SSID/pass, MIDI-clock-out toggle, USB-MIDI cable
  select, `Write & Reboot` (X32Link `.fld`/`.sw`/`.write` styling). `POST /save`
  parses the body, applies via the pure config, validates, saves NVS, reboots.
- **SoftAP fallback** — no stored SSID → `wifi_link` starts AP `KitchenSync-Setup` so
  creds can be set with no serial.
- Verified: save→NVS→reboot→persist round-trip (cable + toggle), live status page.
  Fixed a `strtok_r`-truncates-body bug in the unchecked-checkbox handling.

**Minor follow-up (not blocking):** captive-portal DNS auto-redirect (AP works;
today you browse to 192.168.4.1 manually). Then P4-010 per-output config rows.

## Notes

- Foundation for P4-010 (Multiclock-style per-output clock config).
- SD card present on the device — an option for presets/assets/logs (P4-010
  Multiclock presets) instead of / alongside NVS.
- Consider a first-boot SoftAP + captive portal like X32Link so WiFi can be set
  with no serial (follow-up if not in v1).

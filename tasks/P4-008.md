---
id: P4-008
title: "MIDI transport out — Start/Stop/Continue from Link play state"
status: done
assignee: null
priority: medium
tags: [p4, hub, midi, transport, clock]
depends_on: [P4-005]
created_at: 2026-07-05
updated_at: 2026-07-05
---

## Goal

Send MIDI **transport** messages out the USB-MIDI host alongside the 24-PPQN
clock (P4-005): **Start `0xFA`**, **Stop `0xFC`**, **Continue `0xFB`**, driven by
the Ableton Link session's play/stop state — so downstream gear (arps, sequencers)
runs and stops in lockstep, not just tracks tempo.

## Reuse

The pure `usb_midi_pack.c` already encodes single-byte messages, so
`usb_midi_pack_single(cable, 0xFA/0xFB/0xFC)` is done — this is transport *policy*
(when to emit Start vs Continue vs Stop) + reading Link's start-stop state.

## Scope

- Read Link transport (is-playing / start-stop-sync) from the gossip — confirm
  `link_protocol.c` exposes it, or extend the parser (pure, TDD'd) to surface a
  play/stop flag + start beat.
- Pure transport state machine (TDD'd): Link stopped→playing = Start (or Continue
  if resuming mid-song); playing→stopped = Stop. Emit exactly once per edge.
- Glue: emit the packet via `usb_midi_host_send()`; align Start to the clock so
  gear starts on the beat.

## Deliverable

Pressing play/stop in Ableton starts/stops the attached USB-MIDI gear in sync.
Transport state machine host-tested.

## Progress (2026-07-05) — implemented ✅

- **Link StartStopState parsing** added to `link_protocol.c` — the `stst`
  payload (`isPlaying` 1 byte + `beats` + `timestamp`, both int64 BE; wire format
  confirmed against the Ableton Link SDK `NetworkByteStreamSerializable.hpp`).
  New accessors `link_proto_playing()` / `link_proto_start_stop_seen()`; state
  resets when the session empties. Host-tested (`test_link_protocol` +3).
- **Pure transport state machine** `X32Link/transport.c` (red-green TDD'd,
  `test_transport`): isPlaying edges → START (0xFA) / STOP (0xFC), one per edge;
  **primes on the first real observation** so joining mid-play emits no spurious
  Start (which would jump gear to bar 1). Reuses `usb_midi_pack` for encoding.
- **Wired** into `p4hub_main.c` `clock_out_task` (emits on the configured cable
  alongside the clock, gated on `clock_out_enable`; logs `transport START/STOP`).
- Host suite green; `idf.py build` clean; boot-smoke clean on the P4-NANO.

**Verify (needs a live Link peer):** in Ableton, **press play** → P4 serial logs
`transport START` and the Midihub receives MIDI Start (0xFA); **stop** → `transport
STOP` / 0xFC; attached gear runs/stops in sync. (Continue 0xFB on mid-song resume
is a possible refinement — v1 emits Start on every play edge.)

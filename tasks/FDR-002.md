---
id: FDR-002
title: X32FaderDisp - fader float to dB scribble display
status: done
assignee: null
priority: medium
tags:
  - fader-disp
  - osc
  - esp32
depends_on:
  - FDR-001
created_at: 2026-06-28
updated_at: 2026-06-29
---

## Goal

Implement the FaderDisp behaviour: when a channel fader moves, show its value in
**dB on that channel's scribble strip**, by writing `/ch/NN/config/name`. On a
clean shutdown / disable, restore the original names.

## Acceptance Criteria

- [ ] `fader_disp.{cpp,h}` added to `esp32/X32FaderDisp/`.
- [ ] On each received `/ch/NN/mix/fader ,f <f>`, convert `f`→dB with the
      **exact** piecewise formula in
      [`docs/xr18-xair-osc-cheatsheet.md`](../../docs/xr18-xair-osc-cheatsheet.md)
      ("Fader / level: float ↔ dB"), and send
      `/ch/NN/config/name ,s "<dB>"` (e.g. `"-6.0"`, `"+10"`, `"-oo"`).
- [ ] The float→dB conversion is a **pure host-tested** function
      (`fader_to_db()` in a `.c` or testable unit); Unity test asserts boundary
      cases: `f=1.0→+10`, `f=0.75→0`, `f=0.5→-10`, `f=0.25→-30`, `f=0→-oo`.
- [ ] Per-channel throttle of `FDR_NAME_REFRESH_MS` so a fast fader sweep does
      not flood the mixer.
- [ ] **Name save/restore:** on first sight of a channel, cache its original
      `/ch/NN/config/name`; on disable (web toggle) or graceful stop, write the
      cached names back. Guard with `FDR_RESTORE_ON_EXIT`.
- [ ] Web UI exposes an on/off toggle and channel-count (16/32) selector; value
      persists in NVS via AppConfig.
- [ ] Demoed against the on-device X32 emulator: moving a fader updates the
      corresponding channel `name`; a second client (X32-Edit / Mixing Station)
      shows the dB text. Record in Progress.

## Notes

- Writing `config/name` is **destructive** to user labels — that's why
  save/restore is mandatory, not optional. Cache must survive the session in
  RAM (and ideally LittleFS) so a crash doesn't strand the mixer showing dB
  numbers as names.
- dB string format: 1 decimal for ≥ -10 dB region is fine; clamp to `-oo` at
  the bottom. Keep ≤ 7 chars (scribble width).
- Subscribe to the full `/ch/*/mix/fader` space via `/xremote` (the mixer pushes
  all changes; filter by address prefix in the callback).

## Progress (2026-06-29) — core shipped; web toggle + X32 demo remaining

- `fader_db.{c,h}` (X32 fader law) + `fader_disp.{cpp,h}` added. On
  `/ch/NN/mix/fader ,f` → `fader_db_str()` → write `/ch/NN/config/name ,s`,
  per-channel throttled (FDR_NAME_REFRESH_MS). Originals cached on first sight
  of `/ch/NN/config/name` (queried at begin); `fader_disp_restore()` writes them
  back. Added `osc_listener_send()` so queries/sets reply to the listener socket.
- **8 host tests** (`test_fader_db.c`): the boundary cases (1.0→+10, 0.75→0,
  0.5→-10, 0.25→-30, 0→-oo), a segment join, dB-string formatting + ≤7-char width.
- Firmware compiles (34%).

**Remaining:** (1) FaderDisp-specific web toggle (on/off + 16/32 channel-count)
— currently the shared web_config handles connectivity only; (2) on-device demo
needs an **X32 or the X32 emulator** (XR18 has no scribble strips), so the visible
dB-on-name effect is untested. The osc_in receive path can still be validated on
the XR18 via the serial message log.

## Progress (2026-06-29) — web toggle added; DONE

FaderDisp now has its **own rack-panel config page** (un-symlinked
`X32FaderDisp/web_config.cpp`, same aesthetic as X32·SYNC): On/Off toggle,
Channels 16/32, Mixer Model, IP, WiFi — persisted via NVS. Added
`fdr_enable` + `fdr_chan_count` to the shared `AppConfig` (+ validation, NVS,
DEFAULT_FDR_* in the three config.h). `on_osc` gates the scribble write on
`fdr_enable`; `fader_disp_begin` uses `fdr_chan_count`. 3 new app_config tests.
All three firmwares compile; host suite green.

Only the **on-X32 visible demo** remains (needs an X32 / the emulator) — the
logic + UI are complete and host-tested.

---
id: LNK-025
title: "Headless board profile: run X32Link on Waveshare ESP32-S3-Touch-LCD-1.47 (no display, LCD-backlight beat strobe)"
status: doing
assignee: null
priority: low
tags: [link-firmware, board-bringup, board-profile]
depends_on: []
created_at: 2026-07-02
updated_at: 2026-07-02
---

## Goal

Let the existing **screenless** X32Link firmware be flashed and run on the
**Waveshare ESP32-S3-Touch-LCD-1.47** with a working per-beat LED — **without**
any display/touch/LVGL code. This is the small, standalone board-support slice
that is *not* LNK-014: LNK-014/015/016 are the full on-device display+touch UI;
this ticket is just "make the tempo→OSC bridge boot and blink on this hardware
in hand today."

The wrinkle this solves: the board has **no addressable WS2812** (that's the
non-touch ESP32-S3-LCD-1.47 dongle — different PCB; see LNK-014 Progress). Its
only LEDs are a charger-status LED and an always-on power LED, neither
MCU-driven, and GPIO38 (where the dongle's WS2812 lives) is **LCD_SCL** here.
So the beat indicator is repurposed onto the **LCD backlight enable (GPIO46,
LCD_BL, active-HIGH via NPN)**, driven as a plain digital LED — the panel is
never initialized, so it sits dark and the whole screen strobes on each beat.

## Acceptance Criteria

- [x] `X32Link.ino` gains a board branch `#elif defined(BOARD_WAVESHARE_S3_TOUCH_LCD_147)`
      → `LED_PIN_NUM 46` (plain active-HIGH, **no** `LED_RGB`, no `LED_PWR_PIN_NUM`),
      with a comment documenting the no-WS2812 / GPIO38=LCD_SCL trap.
- [x] Compiles clean for FQBN
      `esp32:esp32:esp32s3:USBMode=hwcdc,CDCOnBoot=cdc,FlashSize=16M,PSRAM=opi`
      (flash 78% / RAM 23%).
- [x] Flashed to a real unit and the per-beat backlight strobe + WiFi-down
      triple-blink both observed working; web UI reachable and `/status` live.
- [x] Screenless board profiles (Super Mini / XIAO / QT Py) unaffected — the
      new branch is purely additive under a new `-D` flag.
- [ ] **Commit decision (blocking to close):** the `X32Link.ino` `#elif` branch
      is the only committable artifact — `build_opt.h` stays **empty at HEAD**
      (per `esp32/AGENTS.md`; set locally to `-DBOARD_WAVESHARE_S3_TOUCH_LCD_147`
      on the unit in hand, do not commit). Land the branch, or decide to hold it.
- [ ] Reconcile the board-flag name with LNK-014. LNK-014's Implementation says
      to add `#define HAS_TOUCH_DISPLAY` to this board's `build_opt.h`; this
      ticket uses `-DBOARD_WAVESHARE_S3_TOUCH_LCD_147`. When LNK-014 lands they
      should be one coherent board profile (one `-DBOARD_*` that also implies
      `HAS_TOUCH_DISPLAY`), not two overlapping flags. Note: once LNK-014 drives
      the real display, GPIO46 becomes the backlight for the UI — the beat
      indicator moves on-screen and this backlight-strobe reverts to a normal
      always-on backlight. So this profile is the **interim, display-less**
      config; don't let both fight over GPIO46.

## Notes

- Verified hardware facts (unit STA MAC `a4:cb:8f:da:df:d0`): ESP32-S3R8,
  16MB flash, 8MB octal (OPI) PSRAM, native USB-CDC. Full schematic-confirmed
  pinout now lives in **LNK-014** (patched same session) — don't duplicate it,
  reference it.
- Beat-LED path is the existing `led_task` in `X32Link.ino` (LNK-021 phase-lock
  + LNK-023 WiFi-down triple-blink); this ticket only points `LED_PIN_NUM` at a
  new pin, no LED-logic changes.
- Serial-over-native-USB is unreliable on this board (documented in
  `esp32/AGENTS.md`); use the web `/status` endpoint to confirm liveness/BPM.
  Firmware advertises no mDNS — find the unit by its Espressif MAC in the
  router's DHCP table (or ARP-scan from a host on the same WiFi).

## Progress

- 2026-07-02 — Implemented and validated on real hardware, **uncommitted** per
  the originating task's "do NOT commit" instruction. Local edits: the
  `X32Link.ino` board branch (above) and `build_opt.h` set to
  `-DBOARD_WAVESHARE_S3_TOUCH_LCD_147` (local-only). Compile 78%/23%; flashed;
  beat strobe + WiFi-down blink confirmed; board joined WiFi (192.168.0.74 at
  time of test) and served `/status`. Remaining: the two open AC items — commit
  the branch, and reconcile the flag name with LNK-014's `HAS_TOUCH_DISPLAY`.

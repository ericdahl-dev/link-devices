---
id: LNK-029
title: "Research: mixer FX-slot introspection + multi-slot tempo control (X32/XR18 + Wing)"
status: todo
assignee: null
priority: medium
tags: [link-firmware, osc, fx, mixer, wing, research, needs-triage]
depends_on: [LNK-005]
created_at: 2026-07-04
updated_at: 2026-07-04
---

## Goal

Scope what the device could do with mixer FX beyond today's single hardcoded
target. Today (LNK-005) we send BPM → `/fx/<slot>/par/01 ,f` (delay-time param)
to **one** configured slot, send-only, assuming the slot holds a delay. This
ticket researches four questions before any implementation:

1. **What can we read from the mixer about the FX slots?**
2. **What other parameters make sense to tempo-control?**
3. **How to handle multiple tempo-syncable effects at once** (e.g. a delay in one
   slot and another effect with its own tap-tempo/rate in another)?
4. **Should we control more than one FX slot?**

Output is a findings doc + a design recommendation, **not** code.

## What we already know (grounding)

- `/fx/N/type ,i` — the loaded **algorithm** per slot, index `[0,60]`
  (`docs/xr18-xair-osc-cheatsheet.md`). This is the key introspection hook.
- `/fx/N/par/MM ,f` — per-algorithm parameters, `MM = 01..64`. Which `MM` is the
  tempo/delay-time param **depends on the algorithm** (par/01 happens to be delay
  time for the delay algorithms we target now).
- We are currently **send-only** and never read `/fx/N/type` — so we're blind to
  what's actually loaded and just assume par/01.
- XR18 has 4 FX slots, X32 has 8 (`config_model_slot_max`).
- The receive/subscribe plumbing exists (`osc_in`, xremote — LNK-013), so reading
  back mixer state is feasible, we just don't use it for FX.
- **Model support today is XR18/X32 only** (`MODEL_XR18=1`, `MODEL_X32=2`;
  `config_model_port` → X32:10023 / XR18:10024). The whole OSC layer assumes the
  **X32-family address tree** (`/fx/N/par/MM`). The **Behringer Wing does not share
  that protocol** — see Q5.

## Research questions

### Q1 — FX-slot introspection
- Read `/fx/N/type` for each slot → know the algorithm. Can we also get a
  human-readable name, or only the index? Build the **index → algorithm** table
  for XR18 and X32 (they may differ).
- For each tempo-relevant algorithm, which `par/MM` is the tempo/delay-time param,
  and what are its **units/encoding** (normalized float → ms? a note-division
  enum? a raw ms value?). Does the XR/X32 FX expose a "tempo sync on/off" or a
  "division" param we should set instead of writing a raw time?
- Can/should we **subscribe** (xremote) so we track slot type changes live and
  avoid writing a delay-time to a slot the user just changed to reverb?

### Q2 — which params are worth controlling
- Catalog tempo-syncable params across algorithms: delay time (delays), LFO rate
  (chorus/flanger/phaser), reverb pre-delay, tremolo/auto-pan rate, gate/ducker
  timing. Note which are musically useful to lock to Link tempo vs noise.
- Non-tempo params worth a look while we're here? (probably out of scope — keep
  this ticket tempo-focused, list others as future.)

### Q3 — multiple tempo-syncable effects at once
- If a user has a delay in slot 1 and, say, a tremolo/tap-tempo effect in slot 3,
  both want the tempo. The device is the tempo authority — we'd write each slot's
  tempo param directly (overriding the mixer's own tap-tempo). Confirm that's the
  right model vs trying to drive the mixer's tap-tempo input.
- Different targets may want **different note divisions** (1/4 vs 1/8-dotted vs
  1/2). Implies a per-target division/multiplier, not one global BPM→ms.
- Encoding differs per algorithm (ms float vs division enum) — the per-target
  mapping must carry the algorithm's param index + unit conversion.

### Q4 — multi-slot control
- Extend config from a single `fx_slot` to a set of targets: each `{slot,
  enabled, division, (auto-detected type → param index + unit)}`.
- Config/UI implications: the web + touch UIs, NVS schema, and validation grow
  from one slot to N. How much of the mapping can be **auto-derived** from
  `/fx/N/type` so the user just picks slots + divisions?
- Rate-limit / OSC-flood concerns when writing several params on every tempo
  change (reuse LNK-011 bpm_publisher send-policy).

### Q5 — Behringer Wing support (different protocol, not a config tweak)
The Wing is a separate research track because it is **not** an X32-family device:
- **Protocol dialect.** The Wing speaks its own control protocol (OSC-style plus a
  text/`$`-style command channel) with a **different address tree, node/param
  model, and encoding** than X32/XR18's `/fx/N/par/MM`. Confirm: the Wing's UDP
  control port(s), whether plain OSC works or the node protocol is required, and
  how FX parameters are addressed and encoded (name-addressed nodes vs numeric
  par indices; value units).
- **FX topology.** Confirm the Wing's FX slot count and effect set (it has more,
  and different "premium" effects than the X32 verb/delay set), and which effects
  are tempo-syncable + how their tempo param is expressed (ms vs beat-division vs
  a sync/tap node).
- **Introspection.** Does the Wing expose richer read-back (effect type/name per
  slot, param metadata) than X32's `/fx/N/type`? If so, auto-detection (Q1) may be
  cleaner on Wing than on X32.
- **Firmware integration.** What it takes to add `MODEL_WING`: a new model id +
  port in `config_model_port`/`config_model_slot_max`, and — crucially — whether
  the X32-specific `osc_out`/`osc_in` layer can be parameterized or needs a
  **separate Wing OSC dialect module** behind the existing `tempo_source`→OSC seam.
  Estimate how much of Q1–Q4 (introspection, param catalog, multi-slot fan-out,
  per-target divisions) is reusable vs Wing-specific.
- **Discovery/connection.** How the device finds/targets a Wing on the LAN vs the
  X32 (mixer IP is user-set today; confirm the Wing is reachable the same way).

Answer Q1–Q4 **independently for the Wing** — do not assume the X32 findings port.

## Deliverables (this is research)

- [ ] `index → algorithm` tables for XR18 and X32, annotated with the tempo param
      index + unit/encoding for each tempo-relevant algorithm.
- [ ] Recommendation: **auto-detect** FX type via `/fx/N/type` (+ optional
      subscribe) vs **manual** per-slot config — with the tradeoffs.
- [ ] A proposed config/UI/NVS shape for N targets with per-target note division.
- [ ] A note on the tap-tempo question (write params directly vs drive tap input).
- [ ] **Wing protocol assessment:** control port(s), address/encoding model, FX
      topology + tempo params, introspection capability, and a `MODEL_WING`
      feasibility estimate — how much of the X32 OSC layer is reusable vs needs a
      separate Wing dialect module behind the `tempo_source`→OSC seam.
- [ ] Follow-on implementation tickets filed from the findings (X32/XR18 and Wing
      likely land as separate implementation tickets given the protocol split).

## Notes

- Rides on LNK-005 (current FX delay send), LNK-011 (send policy), LNK-013
  (`osc_in`/xremote read-back), and the phase/tempo pipeline validated in LNK-020.
- Reference: `docs/x32-osc-protocol.md`, `docs/xr18-xair-osc-cheatsheet.md`,
  `docs/xr18-geq-osc.md`. Hardware to probe: the XR18 (and X32 if available) via
  `osc_in` dumps of `/fx/N/type` and `/fx/N/par/*` while loading different
  algorithms.
- Keep any resulting unit-conversion math (BPM + division → ms/enum) in a pure,
  host-tested module, same pattern as the rest of the firmware.

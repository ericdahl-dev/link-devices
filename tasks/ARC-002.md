---
id: ARC-002
title: "Collapse the measurement-validity leak (active vs committed xform)"
status: todo
assignee: null
priority: high
tags: [architecture, refactor, link, phase, esp32]
depends_on: []
created_at: 2026-07-02
updated_at: 2026-07-02
---

## Goal

Hide the `active` / `xform.valid` distinction behind the measurement seam so
callers can't gate phase validity on the wrong flag. Expose one honest
question — `link_measurement_have_phase_estimate()` (a committed, non-stale
xform exists) — and make `active` an internal detail.

## Problem (from the architecture review)

`link_measurement.h:140-142` exposes two flags: `link_measurement_active()`
(an attempt is in flight) and `current_xform().valid` (a result is committed),
and makes the caller reconcile them. `tempo_source_phase_valid()` reconciled
them **wrong** — it gated on `active()` — which was the "flashing dot" Link bug
(fixed reactively in the Link-phase PR). The header documents the distinction
(`:127-137`) that the caller then ignored. Correctness depends on caller
discipline, not the interface — a leaky seam.

## Acceptance Criteria

- [x] `link_measurement` exposes `bool link_measurement_have_phase_estimate(void)`
      meaning "a committed xform exists (and is fresh enough to trust)".
- [x] `link_measurement_active()` is removed from the public header (or clearly
      marked internal); no caller outside the module reads it. *(Kept for the
      measure pump's lifecycle use — the one legitimate external reader — and
      re-documented in the header as lifecycle-only, never a phase signal. No
      caller gates phase on it.)*
- [x] `tempo_source_phase_valid()` (Link branch) calls
      `link_measurement_have_phase_estimate() && link_proto_timeline(&t)` —
      structurally unable to reproduce the `active`-gated bug.
- [ ] ~~Optional staleness rule~~ **Deferred by design.** Making the pure
      `link_measurement.c` time-aware is a real design change; the periodic
      re-measure + the LNK-026 epoch-reset already keep the xform fresh. Left
      out rather than half-built. Revisit if a stale-estimate symptom appears.
- [x] Host suite green (325 assertions, 0 failures); Link phase HW re-check
      pending on next flash.

## Notes

- Pairs with the periodic re-measure already added (fix/link-phase-validity).
- Overlaps ARC-001's `valid` concern but is a distinct seam: this is the
  measurement module's interface; ARC-001 is the read/publish path.

## Progress

- 2026-07-02 — Filed from the architecture review. The reactive Link-phase fix
  proved the bug; this makes it impossible by construction.
- 2026-07-08 — Implemented on `refactor/arc-002-measurement-phase-estimate`
  (off `master`), TDD. Added `link_measurement_have_phase_estimate()` (returns
  the committed `s_xform.valid`, deliberately independent of `s_active`);
  re-documented `active()` in the header as lifecycle-only; repointed
  `tempo_source_phase_valid()` to the new seam. 5 new host tests pin the
  contract — the key one asserts an in-flight attempt (active) with nothing
  committed reads as NO estimate (the old flashing-dot state). Full suite green
  (325 assertions). Staleness deferred (see AC). HW phase re-check on next flash.

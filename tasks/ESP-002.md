---
id: ESP-002
title: Port X32 emulator to ESP32-S3 Arduino sketch
status: done
assignee: null
priority: high
tags: [port, arduino]
depends_on: []
created_at: 2026-06-26
updated_at: 2026-06-26
---

## Goal

Create the Arduino sketch scaffold and platform seam that wraps src/X32.c for ESP32-S3. Establish native Linux test harness proving seam correctness before hardware deployment.

## Acceptance Criteria

- [x] esp32/X32_emulator/ sketch directory created
- [x] x32_port.h declares seam: x32_get_ip(), x32_apply_defaults(), x32_bind_socket(), X32_STATE_FILE
- [x] x32_port.c implements seam (extern globals, no Arduino deps)
- [x] esp32/tests/test_x32_port.c: 7 native tests all GREEN
- [x] X32.c copied to sketch dir with #ifdef ARDUINO guards on: main(), getmyIP(), #include <ifaddrs.h>
- [x] X32_emulator.ino: WiFi init, LittleFS mount, calls x32_init() + x32_tick() in loop()
- [x] Sketch compiles for XIAO_ESP32S3 without errors

## Notes

**Source**: src/X32.c (5661 lines) — do not modify original; work from copy in esp32/X32_emulator/
**Seam file**: esp32/X32_emulator/x32_port.h + x32_port.c
**Tests**: esp32/tests/ — native gcc, `make run` from that dir
**Three surgical changes to X32.c copy**:
1. `getmyIP()` Linux branch → `#ifdef ARDUINO` calls `x32_get_ip(WiFi.localIP().toString().c_str())`
2. `main()` → `#ifndef ARDUINO` guard; add `x32_init()` + `x32_tick()` functions outside it
3. `fopen(".X32res.rc", ...)` → `fopen(X32_STATE_FILE, ...)`

**X32lib files needed** (fan-in from architecture): Xsprint.c, Xdump.c, X32RpnCalc.c, Xcparse.c, X32logf.c
**X32lib files NOT needed**: X32Connect.c, validateIP4Dotted.c, Xscene2X.c, Testparams.c

**FQBN**: esp32:esp32:XIAO_ESP32S3:PSRAM=opi,FlashSize=8M,PartitionScheme=default_8MB,USBMode=hwcdc

## Progress

2026-06-26: Seam (x32_port.h/c) created. Native test harness created. 7/7 tests GREEN covering IP injection, defaults, file path.
2026-06-26: X32.c copy + X32lib sources copied to sketch dir. Applied ARDUINO guards. X32_emulator.ino scaffold created. Sketch compiles: 43% flash, 21% DRAM (data tables in PSRAM via x32_psram.h / .ext_ram.data section). Native tests still 7/7 GREEN.

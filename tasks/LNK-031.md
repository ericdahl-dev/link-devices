---
id: LNK-031
title: "Deepen: extract the Link measurement-session orchestrator into a pure module"
status: todo
assignee: null
priority: high
tags: [link-firmware, refactor, architecture, testability, measurement]
depends_on: [LNK-018, LNK-026]
created_at: 2026-07-04
updated_at: 2026-07-04
---

## Goal

Pull the measurement-session *policy* out of `link_measurement_io.cpp` (thin
Arduino glue, per ADR-0003) into a **pure, host-tested** state machine. Today
~120 of the file's ~160 lines are branching orchestration logic living in glue
that "isn't meant to be tested" — and **both LNK-026 bugs lived there**, catchable
only by flashing hardware.

## Problem

`link_measurement_io.cpp` claims (header) to be thin glue, but owns:
- `check_trigger()` — peer selection + `different_peer` detection + `due`
  re-measure timer + gating on `link_measurement_active()`.
- `check_epoch_reset()` — origin-jump detection + reset/re-measure response
  (LNK-026 bug 1).
- RX flush in `start_attempt()` (LNK-026 bug 2).
- The 50 ms watchdog / 5-consecutive-timeout retry state machine in `_poll()`.
- 8 `static` state vars (`s_have_ref`, `s_ref_ip/port`, `s_have_prev_ghost`,
  `s_prev_ghost_us`, `s_last_send_us`, `s_consecutive_timeouts`,
  `s_next_measure_us`, `s_have_last_origin`, `s_last_time_origin_us`).

`link_measurement.c` (the "pure" module) only owns the sample buffer + committed
xform — the bug-prone *sequencing* is all in glue.

## Design (not binding)

A pure `link_measurement_session.c`:
- `void session_reset(Session*)`
- `int session_poll(Session*, const SessionInputs* in, SessionAction* out, int max)`
  returning an ordered action list.
- **Inputs** (facts the glue already gathers): `now_us`; peer `{found, ip, port}`;
  timeline `{valid, time_origin_us}`; `samples_count`; `active`; and a pong event
  `{arrived, has_ghost, ghost_us, host_echo_us}`.
- **Actions**: `FLUSH_RX`, `START_ATTEMPT(ip,port)`, `SEND_PING(prev_ghost?)`,
  `END_ATTEMPT(success)`, `RESET_XFORM`.

The glue (`link_measurement_io.cpp`) becomes a mechanical translator: read
peer/timeline/socket facts → `session_poll` → execute the actions (sockets only).
The existing pure `link_phase_timeline_epoch_reset()` and the sample/xform funcs
in `link_measurement.c` stay as-is and are called by the session or the glue.

## Acceptance Criteria

- [x] `link_measurement_session.{h,c}` pure (no Arduino headers), wired into
      `esp32/test/Makefile` with `test_link_measurement_session.c`.
- [x] Host tests cover the LNK-026 regressions as unit tests (15 tests): epoch
      reset emits RESET_XFORM + arms an immediate re-measure; the fresh attempt
      emits FLUSH_RX *first* (stale-pong guard); watchdog re-pings at 50 ms; 5
      timeouts emit END_FAIL; different-peer and due-timer both retrigger; trigger
      gated by `active`; pong carries prev-ghost / commits at READY.
- [x] `link_measurement_io.cpp` reduced to sockets + timing that translate the
      session's actions (`run_action`); behaviour faithfully ported (LNK-026
      fixes preserved as session actions).
- [x] Full host suite green (20 suites); firmware compiles 86%; `AGENTS.md` updated.
- [x] **Re-verified on hardware** (flashed; Link peer @ 127 BPM). Steady play:
      `valid` holds, phase advances at the correct rate, no garbage. **Re-origin:
      user drove an Ableton transport stop/start while watching the LCD phase
      wheel — it holds momentarily (the re-measure window) then re-locks cleanly,
      no erratic garbage (the original LNK-026 symptom).** Wheel sweeps smoothly =
      no loop-blocking regression from the refactor. Note: no distinct "syncing"
      text (the wheel renders the brief `valid:false` as a clean pause). Session
      orchestrator confirmed end-to-end. (HTTP `/status` is too low-throughput to
      catch the ~1-2s syncing window by polling — the LCD wheel is the instrument.)

## Notes

- Highest-leverage deepening finding (2026-07-04 architecture review). Rides on
  ADR-0003. The action-list shape keeps side effects (sockets) in the glue while
  every decision becomes a pure, replayable step.

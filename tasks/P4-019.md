---
id: P4-019
title: "Customize the visual-metronome strip from the web UI (brightness, colors, pattern, fade)"
status: done
assignee: null
priority: low
tags: [p4, hub, led, ws2812, web, realtime]
depends_on: [P4-018, P4-015]
created_at: 2026-07-07
updated_at: 2026-07-07
---

## Goal

Make the WS2812 visual metronome (P4-018) customizable from the web UI, live (no
reboot, via the P4-015 `/live` path).

## Controls

- **Brightness** 0..100 % master scale (also caps 5 V current draw).
- **Pattern** — `Chase` / `Flash` / `Fill` dropdown (the three designs).
- **Fade** 0..100 % — how much a beat dims before the next (sharp blink -> smooth pulse).
- **Colors** — beat + accent, via HTML `<input type=color>` (#rrggbb).

## Implementation

- Pure `metro_strip.c` gains a `MetroStripCfg { beat, accent, bright, mode, fade }`
  and implements all three patterns. Host-tested (`test_metro_strip`, 11 cases
  across chase/flash/fill + brightness + fade + custom colors).
- Config fields `led_brightness / led_mode / led_fade / led_beat_color /
  led_accent_color`; `p4hub_config_set` parses `#rrggbb` (or bare hex) for the
  colors. Validated; `test_led_customization`.
- Web UI `build_led()` renders a "Strip Look" panel (brightness/mode/fade + two
  color pickers), all `class="live"`; `/live` copies the fields; the clock task
  builds `MetroStripCfg` from `g_cfg` each frame.

## Also in this change

- **Form reorganized**: each feature's toggle now heads a collapsible section that
  holds its own settings — MIDI Clock Out → the per-output rows; Metronome →
  accent + VOL/VOICE; LED Strip → Strip Look. Sections hide when their toggle is
  off (`.sect` + `data-when` + `syncSect` JS; server-rendered initial state so no
  flash). Visual pass by the UX agent (section hierarchy, output cards, aligned
  fields, color-picker bars) — presentation only, all field names / `%TOKEN%`
  placeholders / `class="live"` hooks preserved.
- **Transport-stop handling**: the strip goes **off when Link transport is
  stopped** instead of flickering. The Link beat keeps advancing while stopped and
  jumps on a re-origin (playhead move), which jittered the chase. Gate the render
  on `link_proto_playing()`; fall back to show-whenever-locked if the session never
  reports transport.

## Status

Built, host-tested, flashed. Colors default green beat / amber downbeat, 60 %
brightness, chase, 55 % fade — matching P4-018's look.

## Notes

- Adds 5 ints to `P4HubConfig` -> another NVS layout shift; a Write & Reboot after
  flashing rewrites clean (still waiting on P4-014 to end this).

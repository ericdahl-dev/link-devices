#ifndef X32_PSRAM_H
#define X32_PSRAM_H

// X32_DATA is no longer needed; arrays are declared const (flash) + BSS (PSRAM).
// Kept as a no-op to avoid breaking any stray references.
#define X32_DATA

#endif
